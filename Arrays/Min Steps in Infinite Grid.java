public class Solution {
    public int coverPoints(ArrayList<Integer> a, ArrayList<Integer> b) {
        int count=0;
        for(int i=1;i<a.size();i++){
            int max=Math.max(a.get(i),b.get(i));
            
        }
        
    }
}
